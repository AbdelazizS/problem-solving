#include <vector>
using namespace std;

class Solution {
public:
    ReturnType methodName(ParamType param) {
        // Approach:
        // Time: O(?)
        // Space: O(?)

        return result;
    }
};
